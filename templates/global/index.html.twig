{% extends 'base.html.twig' %}

{% block title %} - Accueil{% endblock %}

{% block content %}
  <img class="backgroundimg" src="/img/website/background.jpg" alt="background">
  <div class="content">
      <h1 class="content-title">Catégories</h1>
      <ul class="content-list">
        {% for category in categories %}
          <li class="button"><a href="">{{ category.name | capitalize }}</a></li>
        {% endfor %}
      </ul>
      <h1 class="content-title">Marques</h1>
      <ul class="content-list">
        {% for file in files %}
          <li class="button"><a href=""><img class="button-img" src="/img/brands/{{ file.path }}" alt="marque"></a></li>
        {% endfor %}
      </ul>
  </div>
{% endblock %}